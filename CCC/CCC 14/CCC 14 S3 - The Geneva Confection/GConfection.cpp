#include <bits/stdc++.h>
#define ll long long
#define pii pair<int, int>

using namespace std;

void mountain(stack<int> a, stack<int> b, int c)
{
    if (c==T)
    {
        ok = 1;
        return;
    }

    mountain()
        
}

int T;
void solve()
{
    cin >> T;

    stack<int> tower;
    for (int i=0; i<N; ++i)
    {
        int n;
        cin >> n;
        tower.push(n);
    }
    bool ok;
    mountain()
}


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int N;
    cin >> N;
    while (N--)
        solve();

    return 0;
}