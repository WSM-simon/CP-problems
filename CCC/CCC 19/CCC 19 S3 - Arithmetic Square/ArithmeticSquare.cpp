#include <bits/stdc++.h>
#define ll long long

using namespace std;

const int MxN = 4;
const int MxM = 1e5 + 3;
const int inf = 1e18;

ll grid[MxN][MxN], ver[MxN], hor[MxN];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    for (int i = 1; i < MxN; ++i) {
        for (int j = 1; j < MxN; ++j) {
            ll tem;
            cin >> tem;
            if (tem ==) ver[]
        }
    }
    return 0;
}
